{
  "name": "@invoice-saas/db",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "generate": "prisma generate",
    "validate": "prisma validate",
    "typecheck": "tsc --noEmit",
    "build": "tsc -p tsconfig.json",
    "test": "vitest run",
    "seed": "node prisma/seed.mjs"
  },
  "dependencies": {
    "@invoice-saas/contracts": "*",
    "@prisma/client": "^5.20.0",
    "zod": "^4.2.0"
  },
  "devDependencies": {
    "prisma": "^5.20.0",
    "typescript": "^5.6.3",
    "@types/node": "^22.7.0",
    "vitest": "^2.1.0"
  }
}
